/**
 * Texto ÚNICO de la llamada a la acción de la landing (menú, hero, demo,
 * precios, cierre y barra móvil). Un solo mensaje repetido, no doce.
 *
 * La fuente de verdad es `lib/content.tsx` (Marca); aquí solo se reexporta
 * para que los componentes sigan importando desde `@/lib/cta`.
 */
export { CTA_LABEL } from '@/lib/content';
