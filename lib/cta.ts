/**
 * Texto ÚNICO de la llamada a la acción de la landing (menú, hero, demo,
 * precios, cierre y barra móvil). Un solo mensaje repetido, no doce.
 *
 * TODO(Marca): si `lib/content.tsx` pasa a exportar el texto del CTA,
 * reexpórtalo desde aquí y borra la constante local.
 */
export const CTA_LABEL = 'Empieza gratis 7 días';
